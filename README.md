This project is a simple API service that parses HTTP request headers and extracts useful information such as:

User's IP address.

Browser or application details (User-Agent).

Accepted languages.


It’s designed as a lightweight utility for understanding client requests and can be integrated into larger systems for logging, analytics, or personalization purposes.
